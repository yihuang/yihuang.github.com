============
不动点和递归
============

函数不动点是这么一个值：用它去调用该函数得到的返回值就是它本身，比如 `x` 是函数 `f` 的不动点，就意味着： ::

    f x = x

Haskell 有一个函数叫 `fix` ，它有一个神奇的作用就是，传给它一个函数，它能返回这个函数的不动点，这就是传说中的 Y Combinator 了：

    fix f = x
          = f x
          = f (fix f)

